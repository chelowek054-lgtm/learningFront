# learningFront (клиент) — пример переменных окружения. Скопировать в .env.
# EXPO_PUBLIC_* видны в бандле клиента — СЕКРЕТЫ СЮДА НЕ КЛАСТЬ (инвариант №2).

# Базовый URL backend'а (learningBack)
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
