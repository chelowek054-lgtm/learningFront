// entities/concept — реэкспорт типов графа знаний (определены в shared/api,
// т.к. shared не может импортировать entities).
export type { Graph, GraphNode, GraphEdge, NodeTier } from '@/shared/api';
